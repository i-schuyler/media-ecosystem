# Reserved sanitized Windows evidence directory

No physical Windows evidence has been imported.

The future raw
`media-ecosystem-windows-proof-<UTC timestamp>.zip` remains unchanged outside
Git. Do not place it here. After independent allowlist, checksum, schema,
fixture, source-commit, build-metadata, and privacy verification, add only a
reproducible sanitized report whose provenance includes the whole raw-ZIP
SHA-256.

Tooling output, CI artifacts, screenshots without the structured archive, and
placeholder JSON are not physical evidence.
